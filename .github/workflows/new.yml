name: new

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/new.yml'

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello! My GitHub Actions workflow is working!"

      - name: Show Date
        run: date
