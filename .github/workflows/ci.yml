name: CI
on: push
jobs:
 test:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - run: echo "Runnig tests"
 deploy:
   needs: test
   runs-on: ubuntu-latest
   steps:
    - run: echo "Deploying (only if test pssed)"


